<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
			PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bSQL">


	<insert id="tBoardAction" parameterType="bVO">
		insert into
			tBoard(tbno, tbid, tbtitle, tbbody)
		values(
			(SELECT NVL(MAX(tbno) + 1, 10001) from tBoard),
			#{tbid}, #{tbtitle}, #{tbbody}
		)
	</insert>
	
</mapper>