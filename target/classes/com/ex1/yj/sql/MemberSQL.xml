<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
			PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mSQL">

	<select id="login" resultType="int" parameterType="mVO">
		SELECT
			count(*) cnt
		FROM
			member
		WHERE
			m_id = #{m_id}
			AND m_pw = #{m_pw}
	</select>
	
	<insert id="addMember" parameterType="mVO">
		insert into
			member(m_no, m_id, m_pw, m_name, m_mail, m_tel, m_avt, m_gen)
		values(
			(SELECT NVL(MAX(m_no) + 1, 1001) from member), #{m_id}, #{m_pw}, #{m_name},
			 #{m_mail}, #{m_tel}, #{m_avt}, #{m_gen}
		)
	</insert>
	
</mapper>